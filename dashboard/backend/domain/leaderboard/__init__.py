"""Leaderboard domain package (Phase 3C3).

Canonical home for the leaderboard contest service, baseline plumbing, and the
baseline strategy implementations. The previous locations
(``services/leaderboard_service.py``, ``engines/leaderboard_baselines.py``, and
``engines/strategies/``) remain as thin compatibility re-export shims.
"""
