"""Agents domain package.

Canonical home (Phase 3A1) for the agent and agent-version repositories,
moved from ``dashboard.backend.agent_store`` and
``dashboard.backend.agent_version_store`` (kept as compatibility shims).
"""
